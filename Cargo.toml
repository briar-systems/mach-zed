[package]
name = "mach-zed"
version = "0.10.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
toml = { version = "1.1", default-features = false, features = ["std", "serde", "parse"] }
zed_extension_api = "0.7.0"
